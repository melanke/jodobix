query GamesCreated($where: GameCreated_filter) {
  gameCreateds(where: $where) {
    chainName
    gameId
    creator
    isPrivate
    timestamp
    blockNumber
    blockTimestamp
    transactionHash
  }
}
