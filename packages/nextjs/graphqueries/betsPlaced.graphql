query BetsPlaced($where: BetPlaced_filter) {
  betPlaceds(where: $where) {
    chainName
    gameId
    bettor
    number
    value
    timestamp
    betId
    blockNumber
    blockTimestamp
    transactionHash
  }
}
