query PrizePayments($where: PrizePayment_filter) {
  prizePayments(where: $where) {
    chainName
    gameId
    betId
    bettor
    requestedBy
    drawnNumber
    prizeValue
    timestamp
    blockNumber
    blockTimestamp
    transactionHash
  }
}
