query PrizePayments($where: PrizePayment_filter) {
  prizePayments(where: $where) {
    gameId
    betId
    bettor
    requestedBy
    drawnNumber
    prizeValue
    timestamp
    blockNumber
    blockTimestamp
    transactionHash
  }
}
